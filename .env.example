# Copia este archivo como `.env` y ajusta los valores.
# NOTA: En Vite, solo las variables que empiezan con `VITE_`
# se exponen al código del cliente (import.meta.env).

# URL base de tu API
VITE_API_BASE_URL=http://localhost:3000

# Título de la app
VITE_APP_TITLE=Sistema de Técnicos

# Ejemplo de feature flag
VITE_FEATURE_EXAMPLE=false

# Supabase (requerido para Auth)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
